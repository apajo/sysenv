#!/bin/bash

branch=$(git symbolic-ref HEAD | sed -e "s/^refs\/heads\///")

syadm commit -m "changes from  @ $(date '+%x %X')"
# syadm push --set-upstream origin master
syadm push -u origin "$branch"
