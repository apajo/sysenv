#!/bin/bash
#================================================================
# HEADER
#================================================================
#%  load    - Load all changes in remote

loadtimestamp="$HOME/.config/sysenv/.load";

yadm fetch

touch $loadtimestamp;
